export const state = {
  enabled: false,
  maxCount: 50,
  interval: 1000,
  scrollInterval: 1000,
  autoScroll: true,
  currentCount: 0,
  cleanTimer: null as number | null,
  scrollTimer: null as number | null,
};
